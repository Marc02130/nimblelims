# `.docs/internal/` — working product docs (git-tracked)

Working PRDs, specs, design, ideas, user stories, and SOP packs live here and **are committed**.

This is **not** the published operator how-to tree. Operator how-tos are git-tracked under [`/manuals/`](../../manuals/) (start with [`HOWTO.md`](../../manuals/HOWTO.md)).

**Still gitignored:** [`.docs/manuals/`](../manuals/) — vendor PDFs and legacy local manuals. Do not commit that folder.

**Do not** write new artifacts under the old `.docs-internal/` or `.docs-review/` names.

Parent map: [`.docs/README.md`](../README.md).
