# Atomic Receive CORE — UAT run 2026-08-27

**Verdict:** **CORE Pass**  
**Commit:** `ebac94e` (main)  
**Env:** docker compose local (`lims-db/backend/frontend/r-calculator` healthy)  
**Method:** gstack `/browse` (alice-tech UI) + API (alice / bob / david)  
**SoT script:** [`../uat-atomic-receive.md`](../uat-atomic-receive.md) (stamped)

## Summary

| Area | Result |
|------|--------|
| `/receive` happy path (1 vessel, sticky) | Pass |
| Multi-vessel 1 sample / 3 containers | Pass |
| No analysis picker; `analysis_ids` non-empty → 422 | Pass |
| Available for Testing; samples visible | Pass |
| Zero Tests at receive; later add/delete | Pass |
| Duplicate barcode → 409 | Pass |
| david-cro no Receive + API 403 | Pass |
| alice/bob project isolation | Pass* (404 not 403) |

## Evidence (barcodes used this run)

| Case | Barcodes / actors |
|------|-------------------|
| AR-HV-01 | `NBIO-AR-0010`, `NBIO-AR-0011` → Study-08/09 |
| AR-HV-MC | `NBIO-AR-MC2-P/A1/A2` → Study-10 (3 vessels) |
| AR-HV-05 / CSID | `NBIO-AR-KB-0010` + `CLIENT-UAT-0010` |
| AR-HV-02 | `NBIO-AR-REFUSE-0010` + ELISA id → 422 |
| AR-DUP-01 | Replay `NBIO-AR-0001` / `0010` → 409 |
| AR-TST-* | `NBIO-AR-0009` → Study-15; ELISA add/delete; result then DELETE 400 |
| AR-RBAC-01 | `david-cro` / `david123` |
| AR-MU-01 | bob `CART-AR-0010` on CAR-T; cross → 404 |

Screenshots under `/tmp/uat-*.png` from the browse session (receive empty, HV-01, MC, VAL, samples list, david dashboard).

## Non-blockers → close-out

1. Cross-project receive returned **404** under RLS → fixed to **403** on `fix/atomic-receive-uat-closeout`.
2. `GET /samples/{id}` dropped `client_sample_id` (`SampleBase` omitted field) → fixed on same branch.
3. Script seed barcodes were already present from dogfood; substitutes used as noted in the stamped script.
4. PR **#73** (API-only / “UI not run” stamp) superseded by this full browse+API Pass — closed without merge.

## After CORE — next accessioning packet

| Priority | Packet | Notes |
|----------|--------|-------|
| **1 (recommended)** | **NR-AR-1 results-entry** | Persist lock: typed number → `reported_result` + `qualifiers`; missing `units_default` → 422. New requirements + reviews; not a reopen of CORE. |
| 2 | A-15 / work-plan | Opened later as P1 **asked-for lake** on `/asked-for` (not on receive). See `uat-post-receive-work-spine.md`. |
| 3 | Work orders / WO-* | Processing domain after receive+results. |
| — | Legacy `/accessioning` wizard | **Removed 2026-08-28**; `/accessioning` redirects to `/receive`. |
